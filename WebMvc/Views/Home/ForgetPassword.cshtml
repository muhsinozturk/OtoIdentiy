@using Application.ViewModels

@model ForgetPasswordViewModel

@{
    ViewData["Title"] = "ResetPassword";
}

@section Scripts
{
    @await Html.PartialAsync("_ValidationScriptsPartial")
}


<h4></h4>

@if (TempData["SuccessMessage"] != null)
{
    <div class="alert alert-success" role="alert">
        @TempData["SuccessMessage"]
    </div>
}
<div class="alert alert-info"> Şifre yenileme linki , e-posta adresine gönderilecektir</div>


@* 
ASP.NET Core ModelState doğrulama (validation) hatalarını özet şekilde göstermek için kullanılır.
Yani form gönderildiğinde ModelState.IsValid == false olursa, modeldeki hatalar burada kullanıcıya gösterilir.
*@
<div asp-validation-summary="ModelOnly" class="alert alert-danger" role="alert"></div>


<form method="post">

   
    <div class="mb-3">
        <label asp-for="Email" class="form-label fw-bold"></label>
        <input asp-for="Email" class="form-control" placeholder="E-posta adresiniz" />
        <span asp-validation-for="Email" class="text-danger small"></span>
    </div>


    <button type="submit" class="btn btn-primary w-100">
        <i class="bi bi-box-arrow-in-right"></i> Gönder
    </button>
</form>
